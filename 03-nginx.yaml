- name: install and run nginx
  hosts: web
  become: yes
  tasks:
  - name: installing the nginx
    ansible.builtin.package:
      name: nginx
      state: present
  - name: installing the nginx
    ansible.builtin.service:
      name: nginx
      state: started